spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:3306/spring_batch?characterEncoding=UTF-8&serverTime=UTC
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password:
  batch:
    jdbc:
      initialize-schema: never